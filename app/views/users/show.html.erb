<h1>
  <%= gravatar_for @user %>
  <%= @user.name %>
</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <p><%= "ユーザー名: " + @user.name %></p>
    <% if @user == current_user %>
      <p><%= "メールアドレス: " + @user.email %></p>
    <% end %>
    <p>ロケーション: <%= @user.location.present? ? @user.location : "(設定されていません)"%></p>
  </div>
</div>